apiVersion: v1
kind: Service
metadata:
  name: restaurant-34-pedido
spec:
  type: LoadBalancer
  selector:
    app: restaurant-34-pedido
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000